import './Conference.scss';
import socket from '../../Socket';

const Conference = () => {
	console.log(socket);
	return <div className="translations">Conference</div>;
};

export default Conference;
